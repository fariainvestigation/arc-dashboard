{
  "name": "arc-motion-legal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ARC Motion & Legal module: Cloudflare Worker (D1 + R2), CourtListener integration, Claude drafting gateway",
  "scripts": {
    "test": "node --test --test-force-exit tests/legal.test.mjs",
    "db:migrate": "wrangler d1 execute arc_legal --file=worker/schema.sql --remote",
    "db:migrate:local": "wrangler d1 execute arc_legal --file=worker/schema.sql --local",
    "deploy": "wrangler deploy"
  }
}
