ARC production Worker secrets — set interactively with Wrangler; never store values in this ZIP.

AI Worker:
  GOOGLE_API_KEY
  ANTHROPIC_API_KEY

Legal Worker:
  COURTLISTENER_API_KEY
  ANTHROPIC_API_KEY

Research Worker:
  FIRECRAWL_API_KEY

All four production Workers (case sync, AI, legal, research) use the same D1 database: arc_legal.
Case sync and Legal also bind R2 bucket: arc-legal-files.

Full instructions: PRODUCTION_DEPLOYMENT.md
